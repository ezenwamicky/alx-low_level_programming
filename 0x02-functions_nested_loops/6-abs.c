#include "main.h"
/**
 *_abs - this function prints the absolute value of an integer
 *
 * @n: value of numbber to bbe printed
 *
 *Return: always - < 0 or otherrwise
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
