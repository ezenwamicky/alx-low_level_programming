#include "main.h"
/**
 *mul - finds the multiplication of two ints
 *@a: argument variable
 *@b: argument variable
 *Return: Always return result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return result;
}
