#include "main.h"
/**
 * _abs - computes the absolute value of an integer in a funtion
 * @n: input value
 * Return: value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
